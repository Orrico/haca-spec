# Skill: recon_env

Mapeia o ambiente host e retorna as capacidades disponíveis (OS, binários, RAM).

## Usage
Este skill não aceita parâmetros.

```json
{
  "skill": "recon_env",
  "params": {}
}
```
